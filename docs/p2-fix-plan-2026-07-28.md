# P2 差し戻し修正計画（Phase 7 → Phase 6）

> 作成: 2026-07-28 / 根拠: `docs/review-p2-code-2026-07-28.md`（Request Changes）, `docs/security-audit.md` Phase 2 監査（Critical 0 / High 1）

## 差し戻し理由
- Senior Engineer 判定: **Request Changes**（Must Fix 2件: RV-P2-001 / RV-P2-002）
- Security 判定: **リリースブロッカー 1件**（SEC-009 High）
- CLAUDE.md 品質ゲート「Senior Approve」「Critical/High の未解決なし」を満たさないため P2 未完了。

## 修正スコープ（P2 完了条件）

| # | ID | 深刻度 | 内容 | 対象 |
|---|----|--------|------|------|
| 1 | RV-P2-001 / SEC-010 | Must Fix / Medium | 公開トップの `getLatestNews` に `publishedAt <= now()` の時刻ゲートが無く、予約公開記事が即時露出（F-004違反）。**検証済み関数 `listPublishedNews` は本番経路で未使用**というテスト対象取り違えが根本原因 | `lib/queries.ts`, `lib/news-admin.ts` |
| 2 | RV-P2-002 / SEC-013 | Must Fix / Medium | 本番 `AUTH_SECRET` の起動時強度検証が未実装（P2スコープ未達）。`z.string().min(1).optional()` のまま | `lib/env.ts` |
| 3 | SEC-009 / RV-P2-004 | **High（ブロッカー）** | 管理者ログインに試行回数制御が無く、同期 `scryptSync` により CPU DoS が増幅。E2E flaky の原因でもあった | `lib/password.ts`, `auth.ts`, レート制限基盤（新規） |
| 4 | SEC-011 / RV-P2-005 | Medium | 変更系フォームエンドポイント（save/delete）に CSRF 対策が無く SameSite 既定に暗黙依存 | `app/api/admin/news/save/route.ts`, `delete/route.ts` |
| 5 | SEC-012 | Medium | seed の管理者パスワードにハードコードのフォールバック + 本番実行ガード無し | `prisma/seed.ts` |

### スコープ外（P3以降で対応、今回は着手しない）
- SEC-002(CSP, P5→P3前倒し推奨) / SEC-014〜SEC-017 / SEC-018〜020(Info)
- RV-P2-003, RV-P2-006〜015（Should Fix / Nice to Have）
  - ただし RV-P2-003（authorize のタイミング差）は #3 の scrypt 非同期化と同一ファイルを触るため、副作用なく直せる場合は同時に対応してよい。
  - RV-P2-008（functional-spec §4.10 と renderSafe の乖離）はドキュメント修正のみ。#1〜#5 green 後に対応。

## 進め方（TDD厳守）
1. **Test Agent**: 上記 #1〜#5 の受け入れ条件を満たす失敗テスト（red）を先に追加する。
   - #1 は必ず **本番経路 `getLatestNews`** に対するテストを書く（`listPublishedNews` に書き足すのは誤り。今回の根本原因の再発）。
2. **Impl Agent**: red → green。既存 72 unit / 23 integration / 67 e2e を壊さない。
3. **再レビュー**: Senior 再検収（Must Fix クローズ確認）+ Security 再監査（SEC-009/010/011/012/013 のクローズ確認）。
4. 全 green + Approve + High 0 で **P2 完了**、P3 着手。

## P3 着手前の必須前提（Security 監査より）
- SEC-009 のレート制限基盤は **P3 の未認証エンドポイント（申込/アップロード/チャット）で再利用する前提で汎用化**すること。P3 で作り直さない。
- SEC-013 は管理画面が個人情報にアクセスする P3 で影響が跳ね上がるため P3 前に必須。
